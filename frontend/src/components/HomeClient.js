import React from "react";
import { withRouter } from "react-router-dom";

class HomeClient extends React.Component {
  render() {
    return <span>Home client</span>;
  }
}

export default withRouter(HomeClient);
